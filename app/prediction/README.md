# Prediction Detail Page

Client-rendered prediction result pages under `/prediction/:id`. The page polls
the Prediction API while a result is processing, renders the backend generation
stage and elapsed time, then shows probabilities, sources, and the report.
