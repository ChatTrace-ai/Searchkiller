# Prediction Detail Page

Client-rendered prediction result pages under `/prediction/:id`. The page polls
the Prediction API while a result is processing, then renders probabilities,
reference sources, summary, and the Markdown report.
