# mindmap/ — Mind-Map JSON Stream Route

`POST /api/research/mindmap` — Consumes a session's cached context and streams a structured tree-JSON object via Gemini Pro, formatted for `react-d3-tree` rendering.
